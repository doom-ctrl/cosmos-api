# =============================================================================
# Cosmos API - Environment Configuration
# =============================================================================
# Copy this file to .env and adjust values as needed
# =============================================================================

# -----------------------------------------------------------------------------
# Rate Limiting
# -----------------------------------------------------------------------------
# Maximum requests per IP within the time window
RATE_LIMIT=60

# Time window in milliseconds (60000 = 1 minute)
RATE_WINDOW=60000

# -----------------------------------------------------------------------------
# Cache TTL (Time To Live) in seconds
# -----------------------------------------------------------------------------
# How long to cache responses from the upstream scraper
CACHE_TTL_SEARCH=3600        # Search results: 1 hour
CACHE_TTL_ANIME=86400         # Anime details: 24 hours
CACHE_TTL_EPISODES=86400     # Episode list: 24 hours
CACHE_TTL_SERVERS=3600       # Server list: 1 hour
CACHE_TTL_STREAM=1800         # Stream URLs: 30 minutes

# -----------------------------------------------------------------------------
# Timeouts (in milliseconds)
# -----------------------------------------------------------------------------
REQUEST_TIMEOUT=10000         # General API requests: 10 seconds
STREAM_TIMEOUT=30000          # Stream requests: 30 seconds

# -----------------------------------------------------------------------------
# Retry Configuration
# -----------------------------------------------------------------------------
# Number of retry attempts for failed upstream requests
RETRY_ATTEMPTS=3

# Base delay between retries in milliseconds
RETRY_DELAY=1000

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
# Allowed origins for CORS (use '*' for all, or specify domains)
# Example: CORS_ORIGIN=https://example.com,https://app.example.com
CORS_ORIGIN=*
